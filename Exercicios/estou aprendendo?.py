# Faça um programa que calcule o  reajuste de salario em 15%
#salario_inicial = float(input('Qual seu salario atual? R$'))
#salario_reajustado = float(1.15 * salario_inicial)
#print(f'Seu salario é de {salario_inicial}  e passara a valer ', round(salario_reajustado, 2))

Produto=float(input('Qual o valo do produto? R$'))

#cartao2=float((Produto*0.15)+Produto/2)
#cartao3=float(Produto/3)
#cartao4=float(Produto/4)

#avista=float(Produto*0.95)

#carne = float((Produto*1.15))
#parcelas = float(input('Quantas parcelas serão feitas no carne?'))
#parcela_carne = float(carne / parcelas)

#print(f'O valor no cartão fica  {Produto :.2f}')
#print(f'cartão 2x {cartao2 * 1.10 :.2f}')
#print(f'cartao3')
#print(f'cartao4')

parcelas=float(input('Quantas parcelas você quer fazer?'))
if parcelas>5
    parcelas=(parcelas+(parcelas*0.80))
